# 적용 방법

이 ZIP은 전체 프로젝트가 아니라, 지금 당장 고쳐야 하는 파일만 담은 최소 패치입니다.

## 교체/추가할 파일

- `index.html` → 기존 루트 `index.html`을 이 파일로 교체
- `README.md` → 기존 루트 `README.md`를 이 파일로 교체
- `PROJECT_INDEX.md` → 루트에 새로 추가

## 적용 후 확인할 것

1. 사이트를 새로고침합니다.
2. 상단 탭에 `요일 시간표`가 보이는지 확인합니다.
3. 과목 설정에 다음 필드가 보이는지 확인합니다.
   - 시험일 상태
   - 예상 시험일
   - 시험기간 시작/종료
   - 정규 진도 마감 버퍼
   - 시험 당일 정규 진도 허용
4. `요일 시간표` 탭에서 블록 추가/삭제/저장이 되는지 확인합니다.
5. 샘플 과목 2개로 작은 테스트를 진행합니다.
